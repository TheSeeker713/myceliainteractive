# Production environment variables — baked into the Next.js static bundle at build time.
# NEXT_PUBLIC_* vars are inlined by `next build` and visible in the JS bundle.
# This file is safe to commit — the WSS URL is already public-facing (allUsers on Cloud Run).
NEXT_PUBLIC_GAME_WS_URL=wss://liminal-sin-server-zihz3so5tq-uw.a.run.app

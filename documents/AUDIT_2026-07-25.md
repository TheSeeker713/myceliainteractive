# Full-project audit — 2026-07-25

**Scope:** Agent-side systematic check across theme, device, accessibility, content, infrastructure, and governance.  
**Method:** Source inspection, greps, Vitest suite, live production CDP probes, Cloudflare Workers Builds API, contrast math (same WCAG helpers as F6).  
**Not a build step:** No product code changes in this pass. Findings only; fixes require Jeremy approval.  
**Parallel track:** Jeremy’s human testing pass remains authoritative for visual/UX confirmation.

**Production HEAD verified**

| Check | Result |
|---|---|
| `origin/main` | `a330a000a849f56ba83b6fe4df831ea31a2a8dd6` — *Add near-opaque Darkside inner-card tokens…* |
| Local vs origin | `0	0` (in sync) |
| Cloudflare Workers Build | Latest build **success** for commit `a330a00` (created 2026-07-25T20:13:03Z) |
| Live site | Theme radiogroup present; `--theme-inner-card-bg` resolves to `#101618eb` in Darkside; header pad `12px` at lg+ |

**Vitest (this session):** 25 files / 148 tests passed.

---

## Legend

| Mark | Meaning |
|---|---|
| **PASS** | Checked and looks correct |
| **FAIL** | Clear defect or stale claim |
| **ATTENTION** | Needs Jeremy decision or human re-test |
| **CONFIDENCE** | How sure the agent is (high / medium / low) |

---

## 1. Cross-theme audit (System / Lightside / Darkside)

### 1.1 Pages covered

`/`, `/ls`, `/roadmap`, `/vision`, `/team`, `/contact`, `/privacy`, `/ls/privacy` — theme chrome (header ThemeToggle + tokens) verified live; content surfaces checked via source + selective live probes.

### 1.2 Untokenized / hardcoded color classes

| Finding | Status | Notes |
|---|---|---|
| Team / Roadmap / Vision nested cards | **PASS** | Use `--theme-inner-card-*` (live Darkside card bg `rgba(16,22,24,0.92)`) |
| Button `secondary` | **PASS** | `--theme-chrome-border` / `--theme-control-bg*` |
| Button `primary` `text-white` | **PASS** (intentional) | On accent fill |
| Skip-link `text-white` | **PASS** (intentional) | On accent fill |
| Hero secondary CTA override | **ATTENTION** | `HeroSection.tsx` still hardcodes `border-white/25 bg-white/55 … hover:bg-white/70` on glass — not Darkside-tokenized; may read OK on glass but is the same class of gap |
| `Card.tsx` default / glass variants | **ATTENTION** | Still `bg-white`, `bg-white/10`, `border-white/20` — used by FoldCard/`/ls` architecture; Darkside readability depends on liquid-glass CSS vars, not these Tailwind fills |
| Projects media `bg-black/20` / `bg-black/15` | **PASS** (acceptable) | Video/image frames, not text surfaces |
| `/ls/game/*` black/purple/white chrome | **PASS** (intentional) | Game namespace, separate from site theme |

No remaining `border-black/8 bg-white/*` nested-card pattern on Team/Roadmap (confirmed absent via grep).

### 1.3 Contrast (computed, busy-atmosphere composite)

**Production Team inner card, Darkside** (live CDP, F6-style flatten over busy sample RGB(40,90,100)):

| Text | Ratio | AA 4.5 normal |
|---|---:|---|
| Title | 14.61 | **PASS** |
| Role accent (`#5a9aab`) | 5.53 | **PASS** |
| Body muted | 7.30 | **PASS** |

**F6 Hero LLC (historical, still relevant):**

| Viewport | Mode | LLC min | AA |
|---|---|---:|---|
| 390×844 | normal | 3.928 | **FAIL** (documented; high-contrast escape hatch) |
| 390×844 | high contrast | 6.316 | **PASS** |
| 1280×800 | normal | 7.715 | **PASS** (large text) |

**CONFIDENCE:** High for Team Darkside inner cards (measured live). Medium for exhaustive every-page every-text-node sampling — not every paragraph on `/privacy` / `/ls` was re-measured this session; token plumbing and spot checks look sound.

### 1.4 Theme toggle / System mode

| Item | Status | CONFIDENCE |
|---|---|---|
| Three-state radiogroup in header | **PASS** | High — live `role="radiogroup"` + System/Lightside/Darkside |
| Darkside `data-theme="dark"` tokens apply | **PASS** | High — live |
| Lightside chrome token | **PASS** | High — source + prior session measurements |
| System follows `prefers-color-scheme` | **PASS** (code) | Medium — logic in `themePreference.ts` / bootstrap script; not OS-toggled in this session |

### 1.5 Per-page theme smoke (structure)

| Page | Lightside / Darkside chrome | Nested surfaces | Notes |
|---|---|---|---|
| `/` | **PASS** | Hero glass CTAs **ATTENTION** (hardcoded) | Card stage + theme toggle live |
| `/ls` | **PASS** | Inner cards / secondary Button / diagram tokens **PASS** (source) | Diagram enlarge desktop-only |
| `/roadmap` | **PASS** | Inner-card tokens **PASS** | |
| `/vision` | **PASS** | Horizon inner-card **PASS** | |
| `/team` | **PASS** | Inner-card + contrast **PASS** (measured) | |
| `/contact` | **PASS** | SceneCard text tokens | |
| `/privacy` | **PASS** | Loads; mailto present | Darkside active in probe |
| `/ls/privacy` | **PASS** (route exists) | **CONFIDENCE medium** — not CDP-probed this session |

---

## 2. Cross-device audit (desktop / mobile)

### 2.1 Desktop card system

| Item | Status | Evidence | CONFIDENCE |
|---|---|---|---|
| `MyceliaCardStage` + glitch/`inert` clones | **PASS** | Source: clones `aria-hidden` + `inert` | High (structure) / Medium (runtime animation) |
| Pane hash navigation | **PASS** | `paneHashNavigation.ts` + tests (empty hash → pane 0) | High |
| Focus restore after hash jump | **PASS** | `pendingFocusIdRef` + rAF focus in `MyceliaCardStage` | High (code) / Medium (manual) |
| Live region announcements | **PASS** | `aria-live="polite"` present | High |
| Stage content keyboard scroll region | **PASS** | `LiquidGlassSurface` stage variant: `tabIndex={0}` `role="region"` | High |
| Outside-card wheel / in-card scroll | **ATTENTION** | Covered by unit tests + prior approvals; **not re-driven interactively this session** | Medium |
| Recent theme/header work regressing stage | **PASS** (no evidence of breakage) | Theme/chrome changes isolated from stage machine | Medium |

### 2.2 Mobile card system

| Item | Status | Evidence | CONFIDENCE |
|---|---|---|---|
| Modules present (`cardStageMobileScroll`, drag-follow, guide, image lightbox) | **PASS** | `app/mobile/*`, `app/styles/mobile/*` | High |
| Unit tests for mobile scroll edges / mailto carve-out | **PASS** | `cardStageMobileScroll.test.ts` | High |
| Thumbnails + lightbox | **PASS** (code) | `MobileCardImage` + VV pinning | Medium — no real-device session |
| Arrows / tip guide | **PASS** (code) | card-guide CSS + components | Medium |
| 2D finger tracking / direction | **ATTENTION** | Needs Jeremy real-device pass | Low–medium |

### 2.3 Header / footer edge-pinning

| Item | Status | Evidence |
|---|---|---|
| Desktop `lg+` padding | **PASS** | Live: `padding-left/right: 12px` at 1920px; `--chrome-gutter-x: 12px` |
| Media-query split (no merge with `.site-gutter`) | **PASS** | Built CSS uses `header.site-chrome-x` / `footer.site-chrome-x` under `@media (min-width: 1024px)` |
| Below `lg` keeps body gutter + max-width column | **PASS** | Source in `globals.css` `@media (max-width: 1023.98px)` |
| Exactly at 1024px | **ATTENTION** | Logic correct; **not** pixel-probed at 1023 vs 1024 this session | Medium |

### 2.4 Mobile tilt + onboarding

| Item | Status | Evidence | CONFIDENCE |
|---|---|---|---|
| `MotionOnboardingGate` wired in `SiteMotionShell` | **PASS** | Source | High |
| `MobileTiltParallax` wired | **PASS** | Source | High |
| VV-fixed root on gate | **PASS** | `attachVisualViewportFixedRoot` | High |
| Real permission / tilt feel | **ATTENTION** | Needs device | Low |

---

## 3. Accessibility audit (F1–F12 hold)

Reference: `documents/part15-a11y-closeout.md`, `documents/AUTONOMOUS_BACKLOG_TEST_SUMMARY.md`.

| ID | Topic | Status | Notes |
|---|---|---|---|
| F1–F3 | Keyboard pane nav / focus on hash | **PASS** | Hash jump + focus restore still in `MyceliaCardStage` |
| F4 | Glitch clone Tab trap | **PASS** | `inert` + `aria-hidden` on non-base layers |
| F6 | Hero LLC contrast | **ATTENTION** | Desktop AA OK; **mobile normal still fails** (~3.93) per last F6 harness — high-contrast mode passes |
| F7 | A11y panel focus trap / labelling | **PASS** (code) | Desktop popover + mobile sheet still mounted exclusively via `useIsMobileViewport` |
| F8 | Text-size radiogroup arrows | **PASS** (code) | `AccessibilityPanelBody` |
| F9 | Mobile nav focus / `aria-controls` | **PASS** (code) | |
| F10 | Stage region keyboard scroll | **PASS** | `LiquidGlassSurface` stage content |
| F12 | Live-region pane announce | **PASS** | Present |
| Theme radiogroup | **PASS** | Live: `role="radiogroup"`, `aria-checked`, arrow keys in `ThemeToggle` |
| Skip to content | **PASS** | Live: `#main-content` skip link |
| Image alts | **PASS** with notes | Meaningful alts on logos/projects; decorative `alt=""` on some About/Mission desktop frames (intentional pattern) |

### 3.1 Accessibility panel × theme × platform

| Combo | Status | CONFIDENCE |
|---|---|---|
| Desktop + Lightside | **ATTENTION** | Code path: `AccessibilityPanel` — not fully exercised open→toggle prefs this session |
| Desktop + Darkside | **ATTENTION** | Popover uses `--theme-popover-*` (migrated earlier); needs human open/close check |
| Mobile + Lightside | **ATTENTION** | `AccessibilityBottomSheet` + VV pin — needs device |
| Mobile + Darkside | **ATTENTION** | Same — **highest risk untested intersection** |

---

## 4. Content accuracy audit

### 4.1 Slotsky / fourth wall

| Check | Status | Notes |
|---|---|---|
| Public marketing “Slotsky” | **PASS** | Zero hits in public page TSX/MD |
| Internal game Slotsky | **PASS** (intentional) | `app/ls/game/*`, `game-effects.css`, audio manifest |
| Public “fourth wall” / “4th wall” | **PASS** | Zero public page hits |
| Game `fourth_wall_bells` audio key | **PASS** (intentional internal) | `audioManifest.ts` only |

### 4.2 Trust-system claims

| Location | Status | Notes |
|---|---|---|
| `/ls` hero, experience, slice scope, story sections | **PASS** | Explicitly “planned for MVP / not in this vertical slice” |
| Homepage Projects card (`data.ts` → Liminal Sin) | **FAIL** | Present-tense: *“built around a real-time AI trust and response system”* — contradicts `/ls` framing and prior hold (“don’t silent-fix”). **Needs approved copy change.** |

### 4.3 Character count claims

| Location | Status | Notes |
|---|---|---|
| Prototype cast Jason + Player | **PASS** | Consistent on `/ls` |
| Three-character / Slotsky public claims | **PASS** | Not found in public marketing |

### 4.4 Mailto links (href values)

| Location | href | Status |
|---|---|---|
| Contact | `mailto:contact@myceliainteractive.com` | **PASS** |
| Privacy | `mailto:contact@myceliainteractive.com` | **PASS** |
| Vision CTA | `mailto:contact@myceliainteractive.com` | **PASS** |
| Team | `mailto:adrianna@…` / `mailto:jeremy@…` | **PASS** |
| PrototypeAccessGate / play gate | `mailto:contact@myceliainteractive.com` | **PASS** |

**CONFIDENCE:** High for href correctness. Mailto open behavior is OS/client-dependent — Jeremy should spot-click.

### 4.5 External links

| Link | URL | `target` / `rel` | Status |
|---|---|---|---|
| The S33k3r (nav, contact, projects) | `https://www.thes33k3r.com` | `_blank` + `noopener noreferrer` | **PASS** |
| S33k3r bank (hero CTA) | `https://thes33k3r.com/bank` | `_blank` + `noopener noreferrer` | **PASS** (note: no `www` — intentional if that host redirects) |
| Studio 25 Films (roadmap) | `https://studio25films.com` | `_blank` + `noopener noreferrer` | **PASS** |

---

## 5. Infrastructure / deployment audit

| Item | Status | Notes |
|---|---|---|
| Worker AI binding for FPV/image gen | **PASS** | `wrangler.jsonc` has **D1 + cron + assets only** — no AI binding |
| `/api/ai/image` route | **PASS** (removed) | No `app/api` tree |
| Generative FPV / Workers AI UI | **PASS** (removed) | No `FPVCarousel` (or other FPV) source file in the repo; feature fully torn down from the tree |
| `package.json` AI SDKs | **PASS** | No Workers AI / image-gen deps |
| Cloudflare Worker `myceliainteractive` | **PASS** | Present; last modified 2026-07-25; builds succeeding on `a330a00` |
| Other account Workers (`s25f`, `kaia-checklist`, etc.) | **ATTENTION** | Exist in account; **out of scope** for this site’s FPV teardown — confirm with Jeremy none are leftover MI image pipelines |
| `origin/main` == deployed commit | **PASS** | Builds API commit hash matches `git log origin/main` |
| `.gitignore` Playwright browsers | **PASS** | `.playwright-browsers/` ignored |
| `.gitignore` F6 dumps | **ATTENTION** | Pattern ignores new dumps, but **`f6-contrast-results-1280x800.json` and `…390x844.json` remain tracked** from commit `9b9b1e5` — cleanup would need approval to `git rm --cached` |
| Untracked local `assets/mi_logo.jpg` | **ATTENTION** | Present locally, not committed (fine); don’t commit source JPG unless intended |

---

## 6. Governance file audit

### 6.1 `AGENTS.md`

| Item | Status |
|---|---|
| One-step / approval / checks / commit-push / verify-before-assert | **PASS** — matches current practice |
| Immutable / non-destructive | **PASS** |

### 6.2 `.cursor/rules/agents.mdc`

| Item | Status | Detail |
|---|---|---|
| Stack table (Next 16.2 / React 19 / Tailwind 4 / Vitest / Wrangler) | **PASS** | Matches `package.json` |
| “AI binding” on Worker | **FAIL** (stale) | Line ~23 claims D1 **+ AI binding** + cron; `wrangler.jsonc` has **no AI binding** post-FPV teardown |
| Theme toggle “planned, not shipped” | **FAIL** (stale) | § “Current initiative status” still says no theme toggle in live UI; **ThemeToggle is shipped** with full Darkside tokens |
| Plan Mode / no write without approval | **PASS** | Still accurate |
| Commit/push automatic (aligned with AGENTS.md) | **PASS** | |
| End-of-session: Jeremy external browser confirmation | **PASS** | Still correct |

### 6.3 Related docs (observation only)

| Doc | Status |
|---|---|
| `documents/DECISIONS_AND_DEFERRED.md` | **ATTENTION** — last updated 2026-07-03; still describes separate CDP deploy ceremony and `npm run deploy` as sole path; conflicts with current AGENTS auto-push policy; “resolved” FPV carousel notes describe code that no longer exists in the tree |

**Agents must not edit AGENTS.md / agents.mdc** — flag only (this report).

---

## Priority action list for Jeremy

1. **FAIL — Content:** Approve rewrite of homepage Liminal Sin project blurb in `app/components/studio/data.ts` (present-tense trust system).  
2. **FAIL — Governance (docs only):** Update `.cursor/rules/agents.mdc` theme status + remove AI-binding claim (Jeremy edits by hand).  
3. **ATTENTION — Theme:** Decide whether Hero glass secondary CTA + `Card.tsx` Tailwind whites need token migration.  
4. **ATTENTION — Repo hygiene:** Untrack old F6 JSON dumps under `scripts/a11y/`.  
5. **ATTENTION — Human re-test matrix:** A11y panel open in Darkside on desktop + mobile; mobile card drag/tilt/onboarding; wheel threshold on desktop stage; `/ls/privacy` visual pass.

---

## What this audit did *not* fully do

- Exhaustive WCAG sampling of every text node on every page in both themes  
- Real iPhone/Android tilt, drag-follow, and lightbox save-image flows  
- Opening a11y sheet/popover and toggling every preference in all four platform×theme combos  
- Network proof that `thes33k3r.com/bank` and `studio25films.com` HTTP 200 (href/rel only)

Those are the right focus areas for Jeremy’s parallel human pass.

---

## Errata (2026-07-25, same day)

Path re-verification of every file/path cited in this report:

- **Corrected:** Claims that `app/components/FPVCarousel.tsx` still existed were **false**. Exhaustive case-insensitive search of the working tree and `git ls-files` found **zero** FPV/FPVCarousel files. That ATTENTION item and priority action #4 were removed.
- **Confirmed present:** All other cited source/docs paths exist (`HeroSection.tsx`, `Card.tsx`, `MyceliaCardStage.tsx`, `paneHashNavigation.ts`, `LiquidGlassSurface.tsx`, `SiteMotionShell.tsx`, `MyceliaFlowAtmosphere.tsx`, `VideoBackground.tsx`, mobile modules, theme/a11y files, `data.ts`, `wrangler.jsonc`, F6 result JSONs still tracked, governance docs, etc.).
- **Confirmed absent (as claimed):** `app/api/` tree; `.github/workflows/ci.yml` (CI still deferred — only `.github/copilot-instructions.md` exists under `.github/`).
- **No other factual path errors found** in this re-check.

*End of audit — 2026-07-25*
